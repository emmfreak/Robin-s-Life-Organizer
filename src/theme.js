// Dark black / yellow / white palette + monospace font, used across all pages.
export const colors = {
  bg: '#0d0d0d',        // page background
  surface: '#1a1a1a',   // card/section background — one step lighter than bg
  border: 'rgba(255,255,255,0.15)', // subtle default border/divider on dark surfaces
  black: '#0d0d0d',     // for text/borders on top of yellow (reads as "black ink")
  white: '#ffffff',
  yellow: '#facc15',
  gray: '#9ca3af',      // muted secondary text, legible on dark backgrounds
}

export const font = "'Consolas', 'SFMono-Regular', Menlo, 'Liberation Mono', monospace"
